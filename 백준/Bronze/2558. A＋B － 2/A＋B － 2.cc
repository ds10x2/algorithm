#include <iostream>
#include <algorithm>
using namespace std;

int main() {
	cin.tie(NULL);
	cin.sync_with_stdio(false);

	int a, b;
	cin >> a >> b;
	cout << a + b << "\n";


	return 0;
}